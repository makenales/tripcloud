import { formatData } from 'quickstart/helpers/format-data';
import { module, test } from 'qunit';

module('Unit | Helper | format data');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatData([42]);
  assert.ok(result);
});
